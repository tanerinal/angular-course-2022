import { TrcurrencyPipe } from './trcurrency.pipe';

describe('TrcurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new TrcurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
